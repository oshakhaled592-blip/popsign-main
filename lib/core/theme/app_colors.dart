import 'package:flutter/material.dart';

class AppColors {
  // ━━━━ Brand gradient ━━━━
  static const linear1 = Color(0xFF7C3AED); // violet-600
  static const linear2 = Color(0xFFEC4899); // pink-500

  // ━━━━ Accent ━━━━
  static const accent = Color(0xFF7C3AED);

  // ━━━━ Button text ━━━━
  static const textButtonColor = Color(0xFF1F1D36);

  // ━━━━ Background & Text ━━━━
  static const darkBackground  = Color(0xFF0D0F1A);
  static const grayText        = Color(0xFF94A3B8);
  static const darkGray        = Color(0xFF161929);
  static const moredarkGray    = Color(0xFF111320);
  static const mediumGray      = Color(0xFF252B45);

  // ━━━━ Level Accent Colors ━━━━
  static const a1Color = Color(0xFF04826B);
  static const a2Color = Color(0xFF4B7C0C);
  static const b1Color = Color(0xFFA48C0E);
  static const b2Color = Color(0xFFA44D0E);
  static const c1Color = Color(0xFFDC401D);
  static const c2Color = Color(0xFFDC1D62);
}
